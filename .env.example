# Data volume path (use ./data for local dev, /data for Docker)
DATA_DIR=./data

# Runtime server port
PORT=3001

# Dashboard → Runtime URL (use http://localhost:3001 for local dev, http://runtime:3001 for Docker)
RUNTIME_URL=http://localhost:3001

# CLI backend: claude | codex | gemini
AI_BACKEND=claude

# Agent idle timeout (ms)
IDLE_TIMEOUT_MS=300000

# Max concurrent agents
MAX_AGENTS=10

# Vector DB: lancedb | qdrant
VECTOR_PROVIDER=lancedb

# Qdrant URL (if using Qdrant)
# QDRANT_URL=http://qdrant:6333

# Log level: debug | info | warn | error
LOG_LEVEL=info

# Deployment mode: standalone | managed
MODE=standalone
