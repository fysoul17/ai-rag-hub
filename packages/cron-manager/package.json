{
  "name": "@autonomy/cron-manager",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "clean": "rm -rf node_modules .turbo"
  },
  "dependencies": {
    "@autonomy/conductor": "workspace:*",
    "@autonomy/shared": "workspace:*",
    "croner": "^9.0.0"
  }
}
